# ADR

Architecture Decision Records. One dated file per decision (`YYYY-MM-DD-slug.md`). The index of ADRs lives in [ARCHITECTURE.md](../../ARCHITECTURE.md); this folder holds the full write-up behind each entry.
