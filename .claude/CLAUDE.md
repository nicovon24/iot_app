# graphify
- **graphify** (`.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, use the installed graphify skill or instructions before doing anything else.

# Git commits
Never run `git commit` without the user explicitly asking for it in that turn or session — not even after applying an approved plan or finishing a task the user clearly wanted done. Implementation approval is not commit approval. Always leave changes staged/unstaged and ask "¿querés que lo commitee?" before committing, every time, no exceptions unless the user has just told you to commit.
