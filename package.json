{
  "name": "iot-app",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "prepare": "node -e \"try{require('fs').existsSync('.git')&&require('child_process').execSync('husky',{stdio:'inherit'})}catch(e){}\""
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
